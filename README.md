# Automate the deployment of Oracle Analytics into EC2

* Download (TODO) into `vendor/`.

* Copy `.envrc.template` to `.envrc`, populate. Source using [direnv](https://direnv.net/)

* Run `make terraform`.

* Run `make ansible`.
